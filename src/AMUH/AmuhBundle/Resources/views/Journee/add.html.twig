{% extends "base.html.twig" %}

{% block title %} {{ parent() }} - Journee - Ajouter {% endblock %}

{% block body %} 
    {% for message in app.session.flashbag.get('error') %}
        {{ message }}
    {% endfor %}
    <div>Ajouter un attribut</div>
    <div id="form">
        {{ form_start(form, {'attr': {'class': 'form-horizontal' } }) }}
        
        {{ form_errors(form) }}
        <div class="form-group">
            {# Génération du label. #}
            {{ form_label(form.jouDate, 'Date : ', {'label_attr': {'class': 'col-sm-2 control-label' } }) }}
            
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.jouDate) }}

            <div class="col-sm-10">
              {# Génération de l'input. #}
              {{ form_widget(form.jouDate, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
            
        <div class="form-group">
            {# Génération du label. #}
            {{ form_label(form.typeJournee, 'Type de journée : ', {'label_attr': {'class': 'col-sm-2 control-label' } }) }}
            
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.typeJournee) }}

            <div class="col-sm-10">
              {# Génération de l'input. #}
              {{ form_widget(form.typeJournee, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
            
        <div class="form-group">
            {# Génération du label. #}
            {{ form_label(form.secretaires, 'Secrétaires : ', {'label_attr': {'class': 'col-sm-2 control-label' } }) }}
            
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.secretaires) }}

            <div class="col-sm-10">
              {# Génération de l'input. #}
              {{ form_widget(form.secretaires, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
            
        <div class="form-group">
            {# Génération du label. #}
            {{ form_label(form.medecins, 'Médecins : ', {'label_attr': {'class': 'col-sm-2 control-label' } }) }}
            
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.medecins) }}

            <div class="col-sm-10">
              {# Génération de l'input. #}
              {{ form_widget(form.medecins, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
            
        {# Pour le bouton, pas de label ni d'erreur, on affiche juste le widget #}
        {{ form_widget(form.save, {'attr': {'class': 'btn btn-primary'}}) }}
        
        {# Génération automatique des champs pas encore écrits.
            Dans cet exemple, ce serait le champ CSRF (géré automatiquement par Symfony !)
            et tous les champs cachés (type « hidden »). #}
        {{ form_rest(form) }}

        {# Fermeture de la balise <form> du formulaire HTML #}
        {{ form_end(form) }}
    </div>
{% endblock %}